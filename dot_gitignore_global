# 全局 gitignore（chezmoi 管理：源 dot_gitignore_global → 目标 ~/.gitignore_global）
# 只放"跨所有仓库都不该进版本库"的东西；具体仓库若要反向纳入，用 git add -f。

# 操作系统垃圾
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# 编辑器 / 终端的临时与历史产物（.history/ = VS Code Local History；*.session = 会话缓存）
*.swp
*.swo
*~
Session.vim
.history/
*.tmp
.vscode/
.idea/

# 凭据与私钥类：注意这是全局排除，对**所有**仓库生效（含 chezmoi 源本身），
# 真要纳管其中某个文件必须 git add -f；不想要这层就把下面 8 行删掉。
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
id_rsa
id_ed25519
